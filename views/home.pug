extends layout

include utils

block head
	title= title

block content

	span.toolboxes
		if userId
			h2 My Toolboxes
			each toolbox in userToolboxes
				+toolbox_card(toolbox)
	span.api_cards
		each api in apis
			div.card_container
				+api_card(api)
