extends layout

include utils

block head
	title= title
	link(rel='stylesheet' href='/stylesheets/home.css')
	link(rel='stylesheet' href='/stylesheets/toolbox_card.css')

block content
	.tbc_ctrl_group
		p.tbc_head My Toolboxes
		+toolbox_card
		
	span.api_cards
		each api in apis
			div.card_container
				+api_card(api, toolboxes)
