extends layout.pug

block content
    div
        form(action="/create-toolbox" method="post")
            input(type='hidden', name='_csrf', value=csrfToken)
            label(for="implementation")
            input(type="text", name="implementation")
            button(type="submit") Create Toolbox
